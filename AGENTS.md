# Project Rules

- Do not create Git commits unless the user gives a direct, explicit instruction to commit.
- Do not push to any Git remote unless the user gives a direct, explicit instruction to push.
- When unsure whether a Git operation is allowed, ask the user before acting.
